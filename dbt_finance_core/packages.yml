packages:
  - package: dbt-labs/dbt_utils
    version: 1.1.1
  
  - package: calogica/dbt_expectations
    version: 0.10.1

# Note: dbt_foundation is referenced via dependencies.yml (cross-project ref)
# packages.yml is for hub packages only

